{
  "_comment": [
    "Local values for `sam local start-api` / `sam local invoke`.",
    "Copy to env.json (gitignored) and use THROWAWAY values only.",
    "The key must match the function's logical ID in template.yaml.",
    "When these are set, src/config.ts skips SSM entirely and needs no AWS credentials."
  ],
  "WebhookFunction": {
    "DISCORD_WEBHOOK_URL": "https://discord.com/api/webhooks/000000000000000000/replace-me",
    "VAST_WEBHOOK_SECRET": "local-development-secret-not-a-real-one"
  }
}
